package com.app.blingy.reauty.feature.profile.usecase

import com.app.blingy.reauty.core.domain.model.user.User
import com.app.blingy.reauty.core.domain.repository.FeedRepository
import com.app.blingy.reauty.core.domain.repository.UserRepository
import javax.inject.Inject

//class GetUserUseCase @Inject constructor(
//    private val repo : UserRepository
//) {
//    suspend fun invoke(user: User) = repo.getUser(user)
//}